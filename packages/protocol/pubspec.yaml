name: openstrap_protocol
description: Pure-Dart, multi-band BLE protocol library — bytes <-> records/frames/commands for WHOOP 4/5, plus the Oura ring and generic Bluetooth HR sensors.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
